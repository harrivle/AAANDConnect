{
  "rules": {
    ".read": "auth.token.email.endsWith('@nd.edu') || auth.token.email.endsWith('@alumni.nd.edu')",
    ".write": "auth.token.email.endsWith('@nd.edu') || auth.token.email.endsWith('@alumni.nd.edu')"
  }
}
